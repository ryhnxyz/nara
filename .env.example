# Shared
DATABASE_PATH=./data/nara-bot.db
NARA_DAEMON_URL=http://localhost:4000

# Required for encrypting X tokens / imported wallet secrets.
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY_HEX=

# AI content generation (choose one or both)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# X / Twitter API v2 OAuth app credentials
X_CLIENT_ID=
X_CLIENT_SECRET=
X_REDIRECT_URI=http://localhost:3000/api/x/callback

# Bot behavior
DRAGONBALL_POLL_SECONDS=45
NARACLI_BIN=naracli
